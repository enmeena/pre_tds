name: Email Verification Workflow

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 25ds2000013@ds.study.iitm.ac.in
        run: echo "Hello, world!"
